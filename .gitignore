node_modules/
dist/
coverage/
*.log
*.local
.env
.DS_Store

# Baked narration, produced by `npm run voices`. Large, and specific to
# whichever engine and voice rendered it. Delete these lines if you want to
# commit a voice pack so other people get it too.
public/voice/

# Neural voice model, downloaded once by `npm run voices -- --setup`.
models/
public/dev/
build/
__pycache__/
